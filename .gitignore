# Generated site (transformed by Jekyll)
_site/

# Bundler configuration & artifacts
.bundle/config
Gemfile.lock

# reveal.js (cloned at build time by CI, or on first serve.sh run locally)
reveal.js/
*/reveal.js/

# Local dev assembly directories
.build/
